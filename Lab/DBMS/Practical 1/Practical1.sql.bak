CREATE TABLE REGION(
    REGION_CODE NUMBER(1) NOT NULL,
    REGION_DESC VARCHAR(10) NOT NULL,
    CONSTRAINT REGION_CODE PRIMARY KEY (REGION_CODE),
    CONSTRAINT REGION_CK_DESC CHECK (REGION_DESC IN ('East', 'West', 'North', 'South'))
);